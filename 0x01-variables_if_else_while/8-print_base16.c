#include <stdio.h>
#include <stdlib.h>

/**
 * msin - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
int b = 0 + '0';
while (b <= 9 + '0')
{
putchar(b);
b++
}
char m = 'a';
while (m <= 'f')
{
putchar(m);
m++;
}
putchar('\n');
return (0);
}
